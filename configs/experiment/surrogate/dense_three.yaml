# @package _global_

defaults:
  - override /surrogate: sgd_regression
  - override /surrogate/backbone: dense

exp_name: dense_three

surrogate:
  backbone:
    hidden_neurons:
      - 256
      - 256
      - 256
